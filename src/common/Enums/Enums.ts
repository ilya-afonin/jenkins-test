export enum EMainColorThem {
  //Основной цвет темы.
  green = "#046a38",
  //Цвет индикатора активной вкладки.
  lineColor = "#52A529",
  //Цвет колонки статус в таблице FAIL.
  statusFail = 'rgba(255, 0, 0, .3)',
  //Цвет колонки статус в таблице OK.
  statusOk = 'rgba(0, 255, 98, .3)'
}

export enum ERoutingPath {
  main = "/",
  operations = "/operations",
  directions = "/directions",
  admin = "/admin"
}
